[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "resumeguard-backend"
version = "1.0.0"
description = "Resume Sentinel - AI Fraud Detection Backend"
requires-python = ">=3.10"
